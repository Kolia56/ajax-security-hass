name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install ruff bandit codespell

      - name: Run ruff check
        run: ruff check .

      - name: Run ruff format check
        run: ruff format --check .

      - name: Run bandit security check
        run: bandit -r custom_components/ajax/ -c pyproject.toml

      - name: Run codespell
        run: codespell custom_components/ajax/

  typing:
    name: Typing (mypy)
    runs-on: ubuntu-latest
    permissions:
      contents: read
    # We're at 0 errors under the project's mypy config (see pyproject.toml).
    # The job now blocks merges — flipping --strict for full Platinum
    # strict-typing is the next milestone (see quality_scale.yaml).
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Install dependencies
        run: |
          pip install mypy homeassistant
          pip install -r requirements_test.txt

      - name: Run mypy
        run: mypy custom_components/ajax/

  # tests:
  #   name: Tests
  #   runs-on: ubuntu-latest
  #   permissions:
  #     contents: read
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v6
  #
  #     - name: Set up Python
  #       uses: actions/setup-python@v6
  #       with:
  #         python-version: "3.12"
  #
  #     - name: Install dependencies
  #       run: |
  #         pip install -r requirements_test.txt
  #         pip install -e .
  #
  #     - name: Run tests with coverage
  #       run: pytest tests/ --cov=custom_components/ajax --cov-report=xml --cov-report=term-missing
  #
  #     - name: Upload coverage
  #       if: always()
  #       uses: codecov/codecov-action@v5
  #       with:
  #         file: ./coverage.xml
  #         fail_ci_on_error: false
